# Exact versions the committed numbers were produced with. Python 3.11+.
#
#   pip install -r requirements.txt
#
# pyproject.toml carries looser floors (numpy>=1.26 and so on) for anyone
# installing this as a dependency. This file is the reproducibility pin: the
# combination below is the one that produced the numbers in the committed
# reports. The measured drift history behind the CI tolerances lives in
# scripts/run_check_reproducibility.py.

numpy==2.4.6
pandas==2.3.3
scipy==1.17.1
scikit-learn==1.9.0
xgboost==3.2.0
lifelines==0.30.3
shap==0.51.0
matplotlib==3.11.0

# Development only: test suite, linter, and type stubs for the editor.
pytest==9.1.1
ruff==0.15.20
pandas-stubs==3.0.5.260730
